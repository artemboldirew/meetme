services:
  postgres:
    container_name: shopk
    image: 'postgres:17'
    environment:
      - 'POSTGRES_DB=shopik'
      - 'POSTGRES_PASSWORD=root'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5430:5432'
    volumes:
      - shopik_pgdata:/var/lib/postgresql


volumes:
  shopik_pgdata:
